<%= form_with(url: url, method: :get, local: true) do |f| %>
  <%= f.select :keyword, [ ['投稿が新しい順', 'created_at asc'],
                           ['投稿が古い順', 'created_at desc'],
                           ['期限が近い順', 'deadline asc'],
                           ['期限が遠い順', 'deadline desc'] ], prompt: "選択してください" %>
                           
  <%= f.submit "並べ替え", class: "btn btn-warning" %>
<% end %>